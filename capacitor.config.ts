import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'io.ionic.starter',
	appName: 'app-ng-be-org',
	webDir: 'www',
	bundledWebRuntime: false,
};

export default config;
